
extern fn puts(*Byte): Void = puts;

fn print_first(first: *Byte, second: *Byte): Void {
    puts(first);
}

fn print_second(first: *Byte, second: *Byte): Void {
    puts(second);
}

fn main(): Void {
    print_first(c"first", c"second");
    print_second(c"first", c"second");
}
