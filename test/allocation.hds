import submodule.printable_interface as p;
import hades.memory as mem;

def main(): Void {
    val ptr = new Box(10);
    (*ptr).print();

    mem.free(ptr);

}

implement p.Printable for Box[Int] {
    def print(this: Box[Int]): Void {
        this.value.print();
    }
}

struct Box[T] {
    val value: T;
}

