extern fn puts(*Byte): Void = puts;

fn id(bytes: *Byte): *Byte {
    return bytes;
}

fn first(first: *Byte, second: *Byte): *Byte {
    return first;
}

fn second(first: *Byte, second: *Byte): *Byte {
    return second;
}

fn main(): Void {
    puts(id(b"id"));
    puts(first(b"first", b"second"));
    puts(second(b"first", b"second"));
}
