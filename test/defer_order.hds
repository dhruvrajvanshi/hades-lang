extern fn puts(*u8): Void = puts

fn main(): Void {
    defer {
      print_first(c"2", print_first(c"1", 0))
      void()
    }
}

fn print_first(message: *u8, i: usize): usize {
    puts(message)
    return i
}
fn void(): void {
}
