import hades.marker.{Copy}

def main(): Void {

}

def id[T](value: T): T {
    return id(move value);
}

def s[S](value: S): S where Copy[S] {
    if false {
        val b: Bool = s(true);
        val x = s[Int](10);
        val y: S = id(true);
        val z: S = id(value);
    }
    return value;
}
