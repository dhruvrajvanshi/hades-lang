import submodule.test_utils as utils;
fn main(): Void {
    val fptr: fn(Int) -> Bool = is_greater_than_5;
    utils.print_bool(fptr(65));
}

fn is_greater_than_5(value: Int): Bool {
    return value > 5;
}
