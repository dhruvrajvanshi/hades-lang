
fn main(): Void {
    match X.Y(b"PASS") {
        Y(val message) -> {
            apply || {
                puts(message)
            }
        }
    }
}

fn apply(f: || -> void): Void {
    f()
}

enum X {
    Y(*cchar)
}
extern fn puts(*cchar): Void = puts
