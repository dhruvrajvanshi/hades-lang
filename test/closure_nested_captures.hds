
def main(): Void {
    val x = b"1"
    apply || {
        puts(x)
        val y = b"2"
        apply || {
            puts(y)
        }
    }
}

def apply(f: || -> Void): Void {
    f()
}

extern def puts(*u8): Void = puts
