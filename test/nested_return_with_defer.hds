import libc as c;

fn main(): Size {
    defer { print(b"1") }
    if true {
        defer { print(b"2") }
        if true {
            defer { print(b"3") }
            print(b"4");
            return 0;
        }
        return 1;
    }

    return 1;
}
fn print(msg: *u8): void {
    c.puts(msg)
}