import lib.assert as Assert;
import lib.string_slice as Slice;
import lib.string as String;
const str = String.new_from_c;
const assert = Assert.assert;

def main(): Void {
    val _a = str(b"test");
    val _b = str(b"test");
    val _c = str(b"test1");
    val a = (&_a).as_slice();
    val b = (&_b).as_slice();
    val c = (&_c).as_slice();
    assert(a.equals(b));
    assert(not a.equals(c));
}